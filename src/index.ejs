<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Single-SPA Showcase</title>
  <meta http-equiv="Content-Security-Policy" content="default-src 'self' https: localhost:*; script-src 'unsafe-inline' 'unsafe-eval' https: localhost:*; connect-src https: localhost:* ws://localhost:*; style-src 'unsafe-inline' https:; object-src 'none';">
  <meta name="importmap-type" use-injector />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
        'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
        sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      background: #282a36;
      color: #f8f8f2;
    }

    #shell-container {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
    }

    #main-content {
      margin-top: 80px; /* Space for shell header */
      min-height: calc(100vh - 140px); /* Account for header + bottom progress bar */
      padding: 2rem;
      padding-bottom: 80px; /* Space for bottom progress bar */
      background: #282a36;
    }

    #progress-bar-portal {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 1000;
    }
  </style>
  <!-- If you wish to turn off import-map-overrides for specific environments (prod), uncomment the line below -->
  <!-- More info at https://github.com/single-spa/import-map-overrides/blob/main/docs/configuration.md#domain-list -->
  <!-- <meta name="import-map-overrides-domains" content="denylist:prod.example.com" /> -->

  <!-- Shared dependencies go into this import map -->
  <script type="injector-importmap">
    {
      "imports": {
        "single-spa": "https://cdn.jsdelivr.net/npm/single-spa@6.0.3/lib/es2015/esm/single-spa.min.js",
        "react": "https://esm.sh/react@19.0.1/",
        "react-dom": "https://esm.sh/react-dom@19.0.1/",
        "react-dom/client": "https://esm.sh/react-dom@19.0.1/client/"
      }
    }
  </script>
  <link rel="preload" href="https://cdn.jsdelivr.net/npm/single-spa@6.0.3/lib/es2015/esm/single-spa.min.js" as="module">

  <!-- Production import map -->
  <script type="injector-importmap">
    {
      "imports": {
        "@<%= orgName %>/root-config": "https://single-spa.showcase.zanzapla.net/<%= orgName %>-root-config.js",
        "@<%= orgName %>/shared-libs": "https://zanzapla.net/showcase-shared-libs/<%= orgName %>-shared-libs.js",
        "@<%= orgName %>/shell": "https://zanzapla.net/showcase-shell/<%= orgName %>-shell.js",
        "@<%= orgName %>/intro": "https://zanzapla.net/showcase-intro/<%= orgName %>-intro.js",
        "@<%= orgName %>/content": "https://zanzapla.net/showcase-content/<%= orgName %>-content.js",
        "@<%= orgName %>/qa-extra": "https://zanzapla.net/showcase-qa-extra/<%= orgName %>-qa-extra.js"
      }
    }
  </script>

  <script src="https://cdn.jsdelivr.net/npm/import-map-overrides@5.1.1/dist/import-map-overrides.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@single-spa/import-map-injector@2.0.1/lib/import-map-injector.js"></script>
</head>
<body>
  <noscript>
    You need to enable JavaScript to run this app.
  </noscript>

  <!-- Shell: Always visible (header + source of portalled progress bar) -->
  <div id="shell-container"></div>

  <!-- Main content area for routed MFEs -->
  <main id="main-content"></main>

  <!-- Portal target for progress bar (rendered by shell MFE) -->
  <div id="progress-bar-portal"></div>

  <script>
    window.importMapInjector.initPromise.then(() => {
      import('@<%= orgName %>/root-config');
    });
  </script>
  <import-map-overrides-full show-when-local-storage="devtools" dev-libs></import-map-overrides-full>
</body>
</html>
